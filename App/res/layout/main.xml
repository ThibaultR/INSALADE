<?xml version="1.0" encoding="utf-8"?>
<!--
@author Hyukchan Kwon (hyukchan.k@gmail.com)
@author Thibault Rapin (thibault.rapin@gmail.com)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:paddingTop="@dimen/mainVerticalPadding"
              android:paddingBottom="@dimen/mainVerticalPadding"
              android:background="@color/mainColor"
        >
    <ImageView
            android:layout_width="fill_parent"
            android:layout_height="@dimen/hHeader"
            android:src="@drawable/header_logo"
            android:layout_marginBottom="@dimen/elementVerticalMargin"
            android:onClick="onClickHeaderLogo"
            />
    <android.support.v4.widget.SwipeRefreshLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/swipe_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <ScrollView
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                >
            <LinearLayout
                    android:id="@+id/dayList"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    >
            </LinearLayout>
        </ScrollView>
    </android.support.v4.widget.SwipeRefreshLayout>
</LinearLayout>

